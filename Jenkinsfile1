pipeline {
    agent any

    stages {
        stage('LMS code analysis') {
            steps {
                echo 'Preparing Sonar analysis'
               sh 'cd webapp && sudo docker run --rm -e SONAR_HOST_URL="http://15.207.114.120:9000/" -v ".:/usr/src" -e SONAR_TOKEN="sqp_38f9791cbc033c58886370e23cf38f211540af7b" sonarsource/sonar-scanner-cli -Dsonar.projectKey=lms'
                echo 'completed Sonar analysis. Check for Bugs'
            }
        }

        stage('LMS Build artifacts') {
            steps {
                echo 'Preparing LMS build'
                sh 'cd webapp && npm install && npm run build'
                echo 'Completed LMS build with artifact'
            }
        }

        stage('LMS release Artifacts')
             {
            steps {
                echo 'Preparing LMS release'
                echo 'Completed LMS release'
            }
        }

        stage('LMS Deploy Artifacts') {
            steps {
                echo 'Preparing LMS Deployment'
                echo 'Completed LMS deployment'
            }
        }

        stage('LMS Clean up') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}